import{Nav}from'@/components/layout/nav';export default function Settings(){return <div className="flex"><Nav/><main className="max-w-3xl p-8"><h1 className="text-3xl font-bold">Settings</h1><div className="mt-6 rounded-2xl bg-white p-6 shadow-sm space-y-3"><p>Gemini API keys are supplied in the chat UI per session and never stored permanently.</p><p>Models are centralized with GEMINI_MODEL, GEMINI_FAST_MODEL, and GEMINI_EMBEDDING_MODEL.</p><p>Optional web fallback uses a Tavily API key supplied in the chat UI for the current session.</p><p>Thresholds default to upper 0.7 and lower 0.3.</p></div></main></div>}
