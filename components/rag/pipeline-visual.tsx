'use client';import {motion}from'framer-motion';const steps=['Retrieve','Evaluate','Route','Correct','Refine','Generate'];export function PipelineVisual({active='Generate'}:{active?:string}){return <div className="flex flex-wrap items-center gap-2">{steps.map((s,i)=><motion.div key={s} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:i*.08}} className={`rounded-2xl border px-4 py-3 shadow-sm ${s===active?'bg-indigo-600 text-white':'bg-white'}`}>{s}</motion.div>)}</div>}
